// @flow

type State = [];
type Action = { type: "" };

function books(state: State = [], action: Action = { type: "" }): State {
  switch (action.type) {
    default:
      return state;
  }
}

export default books;
